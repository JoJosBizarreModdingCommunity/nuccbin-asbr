/*use binrw::{binrw, BinReaderExt, BinWriterExt, NullString};
use binrw::io::{Cursor, Seek,SeekFrom};
use serde::{Serialize, Deserialize};


use super::{NuccBinaryParsed, NuccBinaryType};

#[binrw]
#[derive(Serialize, Deserialize, Debug)]
pub struct Entry {
    #[serde(skip)]
    pub charviewer_id_pointer: u64,

    pub part: i32,
    pub menu1_index: i32,
    pub menu2_index: i32,
    pub unk0: i32,

    #[serde(skip)]
    pub modelcode_pointer: u64,

    #[serde(skip)]
    pub xfbin_pointer: u64,

    #[serde(skip)]
    pub face_anm1_pointer: u64,

    #[serde(skip)]
    pub face_anm2_pointer: u64,

    #[serde(skip)]
    pub bas_anm_pointer: u64,
*/
